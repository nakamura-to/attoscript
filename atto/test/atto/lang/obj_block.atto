person =
	name: 'hoge'
	addr:
		city: 'Tokyo'
		street: 'foo'
	age: 20

assert(person.name == 'hoge', 'name')
assert(person.addr.city == 'Tokyo', 'addr')
assert(person.addr.street == 'foo', 'street')
assert(person.age == 20, 'age')
